import { TaskStatusClassPipe } from './task-status-class.pipe';

describe('TaskStatusClassPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskStatusClassPipe();
    expect(pipe).toBeTruthy();
  });
});
